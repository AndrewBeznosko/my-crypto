// import cookie from 'js-cookie';
window.axios = require('axios');

// let bearer = cookie.get('access_token');

// if (bearer) {
//     window.axios.defaults.headers.common['Authorization'] = bearer
// }
